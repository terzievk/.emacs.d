# my emacs config
### Completion is a futile illusion. This is a system. It grows, get's old, get's updated, and get's improved as time goes by.

Tip: when downloading for the first time clear all the start-up errors because you might be missing some packages otherwise.

Tip2: for C++ you should install the irony mode server(M-x irony-install-server). But first:
- get the irony dependancies handled (for Arch clang and llvm)
- there's a know issues with finding the irony version, see:
https://github.com/Sarcasm/irony-mode/issues/592

I recently temprarily commented out a few packages. Here is the list:
- nlinum-relative (deprecated)
- sudo-edit
- autopair (deprecated)
- agda
